# Figures 🖼️

This folder contains promoted, publication-facing images. Immutable run outputs and their CSV/JSON/PDF sidecars remain under `outputs/` so provenance checks can compare every promoted image with its generating run.

Regenerate figures through `python main.py run all-figures ...`; do not edit accepted PNG files manually.

