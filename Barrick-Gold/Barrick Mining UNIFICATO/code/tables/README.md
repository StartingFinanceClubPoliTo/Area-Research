# Tables 📋

`publication_table_index.csv` is the handoff map for the machine-readable tables used by the paper and unified thesis. It points to authoritative run outputs instead of duplicating mutable CSV files.

Add a row only after a table's source run and interpretation status are verified.

