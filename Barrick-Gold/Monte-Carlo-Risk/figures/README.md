# Figures

This folder is reserved for charts generated by the reproducibility script.

The script currently writes diagnostic plots for random-number generation, Monte Carlo option-pricing convergence, quasi-Monte Carlo Asian option pricing, and terminal wealth simulation.

Regenerate the files from the project root with:

```bash
python examples/run_reproducibility.py
```
