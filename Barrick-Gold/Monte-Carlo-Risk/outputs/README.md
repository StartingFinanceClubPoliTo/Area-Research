# Outputs

This folder is reserved for CSV summaries generated by the reproducibility script.

The main output table records representative numerical results for the article examples, including Monte Carlo prices, confidence intervals, variance-reduction estimates, mixture-return moments, and portfolio risk metrics.

Regenerate the files from the project root with:

```bash
python examples/run_reproducibility.py
```
